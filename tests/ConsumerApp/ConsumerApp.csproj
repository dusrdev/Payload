<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="ParentPackage" Version="0.1.0-alpha" />
  </ItemGroup>

  <ItemGroup>
    <!-- Toggle to true to stop future synchronization for this tag. Existing files remain. -->
    <PayloadPolicy Include="ParentPackage" Tag="FluentValidationSkill" Disable="false" />
  </ItemGroup>
</Project>
